InputFilepaths:
  simPath: "/cosma8/data/dp004/colibre/Runs/{simName}"
  skiFilepath: "/Users/agebek/Projects/Colibre/colibre-skirt/template_v{skifileversion}.ski"

  catalogueFile: "{simPath}/SOAP/halo_properties_{snap_nr:04d}.hdf5"

  # If the snapshots are distributed
  virtualSnapshotFile: "{simPath}/SOAP/colibre_with_SOAP_membership_{snap_nr:04d}.hdf5"

  # If not distributed you need to provide the membership and snapshot information separately
  snapshotFile: "{simPath}/snapshots/colibre_{snap_nr:04d}.hdf5"
  membershipFile: "{simPath}/SOAP/membership_{snap_nr:04d}.hdf5"

OutputFilepaths:
  sampleFolder: "/cosma/home/do019/dc-gebe1/" # Folder where the galaxy sample .txt files are stored.
  storeParticlesPath: "/cosma/home/do019/dc-gebe1/" # Folder where the .txt particle files are stored
  SKIRTinputFilePath: '/Users/agebek/Downloads/' # Path where the SKIRT input files will be stored


